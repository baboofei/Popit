<?php
    function logout($name, $identifier)
    {
	/* TODO: Write the function */
    }

    function logoutCheckIdentifier($name, $identifier)
    {
	/* TODO: Write the function */
    }

    function loginRmvIdentifier($name)
    {
	/* TODO: Write the function */
    }

    $username = $_GET["un"];
    $identifier = $_GET["id"];

    logout($username, $identifier);
?>
